{% extends 'base.html.twig' %}

{% block title %}AffectationBadge
{% endblock %}

{% block content %}
	<h1>AffectationBadge</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ affectation_badge.id }}</td>
			</tr>
			<tr>
				<th>Badge</th>
				<td>{{ affectation_badge.idbadge.libelle }}</td>
			</tr>
			<tr>
				<th>Date affectation</th>
				<td>{{ affectation_badge.dateaffectation ? affectation_badge.dateaffectation|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_affectation_badge_index') }}">back to list</a>

	<a href="{{ path('app_affectation_badge_edit', {'id': affectation_badge.id}) }}">edit</a>

	{{ include('affectation_badge/_delete_form.html.twig') }}
{% endblock %}
